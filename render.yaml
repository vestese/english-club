services:
  - type: web
    name: club-anglais-accra
    env: python
    region: frankfurt
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -k gevent -w 1 qserver:app
    envVars:
      - key: PORT
        value: 10000
